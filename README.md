
On the display machine:

* `make`
* `cd node`
* `npm install`
* `cp .env.sample .env`
* `npm run start`

On the server:

* `cd ../bin/planet-word.voice-prototype`
* `npm install`
* `cp .env.sample .env`
* `npm run start`
